#include <iostream>

int main(int argc, char* argv[])
{
	for (int i = 1; i < 6 ; ++i)
	{
		for (int j = 1; j < 6; ++j)
		{
			bool k = i == 2 || i == 4;
			std::cout << abs (j - k * 6);
		}
		std::cout << std::endl;
	}
	return EXIT_SUCCESS;
}
