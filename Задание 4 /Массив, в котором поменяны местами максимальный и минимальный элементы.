#include <iostream>

int n = 0;
std:: cin >> n;
int* a = (int*)malloc(sizeof(int) * n);
void swap(int* a, int* b)
{
	int t = *(a + (n - 1));
	*(a + (n - 1)) = *(a + 0);
	*(a + 0) = t;
}

int main(int argc, char** )
{
	int k = 0;
	int m = 0;
	std::cin << k;
	std::cin << m;
	swap(&k, &m);
	std::cout << k;
	std::cout << " ";
	std::cout << m;
	std::cout << std::endl;
	return EXIT_SUCCESS;
}
